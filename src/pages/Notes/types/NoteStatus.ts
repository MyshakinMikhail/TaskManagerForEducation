export type NoteStatusType = "in-progress" | "completed" | "overdue";

export type ConvertedNoteStatuses = "В процессе" | "Выполнено" | "Просрочено";

export const NoteStatuses = ["in-progress", "completed", "overdue"];
